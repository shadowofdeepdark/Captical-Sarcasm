# 夸张因素分析 - 代码修改说明

## 📋 概述

这份文档详细说明了如何在原始讽刺分析中加入**夸张(Exaggeration)**因素的分析。新分析文件：`2025.5.6_analysis_with_exaggeration.qmd`

---

## 🆕 主要修改和新增内容

### 1. **加载夸张信息** (第 62-79 行)

```r
# 从stimuli.csv读取critical region和Exaggeration列
stimuli <- read_csv('stimuli.csv') |>
  rename(itemid = item_id, critical_region = `critical region`) |>
  select(itemid, critical_region, Exaggeration)  # [新增] 加入Exaggeration列

# 将Exaggeration转换为因子，并设置有意义的标签
edata <- edata |>
  mutate(exaggeration = factor(Exaggeration,
                               levels = c(0, 1),
                               labels = c("non-exaggerated", "exaggerated")))
```

**功能说明:**
- 从stimuli.csv中读取`Exaggeration`列（0 = 无夸张，1 = 有夸张）
- 转换为因子变量，标签为"non-exaggerated"（无夸张）和"exaggerated"（有夸张）
- 检查数据集中有多少条目是夸张的，多少是非夸张的

---

### 2. **描述性统计：按夸张分组** (第 133-156 行)

```r
desc_stats <- edata |>
  group_by(format, type, exaggeration) |>
  summarise(
    n = sum(!is.na(RT)),
    mean_RT = mean(RT, na.rm=TRUE),
    sd_RT = sd(RT, na.rm=TRUE),
    se_RT = sd_RT / sqrt(n)
  )
```

**输出内容:**
- **按format（直接/间接）、type（字面/讽刺）、exaggeration（有/无夸张）分组**
- 计算每组的：
  - 样本量 (n)
  - 平均反应时间 (mean_RT)
  - 标准差 (sd_RT)
  - 标准误 (se_RT)

**讽刺效应计算:**
```r
sarcasm_effect = sarcastic - literal
```
- 正值 = 讽刺句比字面句读得慢（处理更困难）
- 负值 = 讽刺句比字面句读得快

---

### 3. **可视化：夸张效应图** (第 158-197 行)

#### 图1：交互图 (Type × Exaggeration)
```r
ggplot(desc_stats, aes(x = type, y = mean_RT, color = exaggeration, group = exaggeration)) +
  geom_line() +
  geom_point() +
  geom_errorbar(...) +
  facet_wrap(~format)
```

**显示内容:**
- X轴：Type（literal vs sarcastic）
- Y轴：平均反应时间
- 颜色：Exaggeration（exaggerated vs non-exaggerated）
- 分面：Format（direct vs indirect）

**如何解读:**
- **平行线** = 无交互效应（夸张对讽刺处理无影响）
- **交叉线** = 有交互效应（夸张改变了讽刺处理模式）

#### 图2：讽刺效应柱状图
```r
ggplot(sarcasm_effect, aes(x = format, y = sarcasm_effect, fill = exaggeration)) +
  geom_col(position = "dodge")
```

**显示内容:**
- 直接对比有夸张和无夸张条件下的讽刺效应大小
- 0线以上 = 讽刺比字面难处理
- 0线以下 = 讽刺比字面容易处理

---

### 4. **统计模型：原始模型** (第 199-216 行)

```r
model_original <- buildmer(
  RT ~ format*type + (format*type|id) + (format*type|item_name),
  data = edata
)
```

**模型说明:**
- 这是**原始模型**（不包括exaggeration）
- 固定效应：`format * type` (2×2设计)
- 随机效应：参与者和项目的随机斜率和截距

---

### 5. **扩展模型：加入夸张因素** (第 218-256 行)

#### 模型A：三因素完全交互模型
```r
model_exaggeration <- buildmer(
  RT ~ format * type * exaggeration +
    (format * type | id) +
    (1 | item_name),
  data = edata
)
```

**模型包含的效应:**
1. **主效应 (Main Effects):**
   - `format`: 直接 vs 间接陈述
   - `type`: 字面 vs 讽刺
   - `exaggeration`: 有夸张 vs 无夸张

2. **二阶交互 (2-way Interactions):**
   - `format:type`: 陈述方式是否影响讽刺处理
   - `format:exaggeration`: 陈述方式是否影响夸张处理
   - `type:exaggeration`: **核心问题！夸张是否影响讽刺处理**

3. **三阶交互 (3-way Interaction):**
   - `format:type:exaggeration`: 夸张对讽刺的影响是否取决于陈述方式

#### 模型B：聚焦模型（Type × Exaggeration）
```r
model_type_exag <- buildmer(
  RT ~ type * exaggeration + format +
    (type * exaggeration | id) +
    (1 | item_name),
  data = edata
)
```

**用途:**
- 专注于**type × exaggeration交互**
- 更简洁，收敛性更好
- 如果主要关心"夸张如何影响讽刺处理"，这个模型更合适

---

### 6. **项目层面分析** (第 258-300 行)

```r
item_analysis <- edata |>
  group_by(item_name, exaggeration, format, type) |>
  summarise(mean_rt = mean(RT, na.rm=TRUE)) |>
  pivot_wider(names_from = type, values_from = mean_rt) |>
  mutate(sarcasm_effect = sarcastic - literal)
```

**功能:**
- 计算**每个项目**的讽刺效应
- 分别显示夸张和非夸张项目
- 识别哪些项目显示出最强的讽刺效应

**输出表格示例:**
| item_name | exaggeration | literal | sarcastic | sarcasm_effect |
|-----------|--------------|---------|-----------|----------------|
| library   | exaggerated  | 450     | 520       | 70             |
| babysit   | non-exag     | 430     | 425       | -5             |

---

### 7. **过滤"好"项目** (第 302-338 行)

```r
good_items <- item_analysis |>
  filter(format == "direct", sarcasm_effect > 0) |>
  pull(item_name)

edata_filtered <- edata |> filter(item_name %in% good_items)
```

**逻辑:**
1. 筛选出讽刺效应 > 0 的项目（符合预期：讽刺更难处理）
2. 查看"好"项目中有多少是夸张的，多少是非夸张的
3. 仅用"好"项目重新运行模型

**目的:**
- 排除"不好"的刺激材料（可能设计有问题）
- 在高质量项目上测试夸张效应

---

### 8. **直接对比：配对t检验** (第 340-369 行)

```r
# 计算每个参与者在两种条件下的讽刺效应
participant_effects <- edata |>
  filter(format == "direct") |>
  group_by(id, exaggeration, type) |>
  summarise(mean_rt = mean(RT, na.rm=TRUE)) |>
  pivot_wider(names_from = type, values_from = mean_rt) |>
  mutate(sarcasm_effect = sarcastic - literal)

# 配对t检验
t_result <- t.test(exag_effects, non_exag_effects, paired = TRUE)
```

**功能:**
- 直接比较同一参与者在**有夸张 vs 无夸张**条件下的讽刺效应
- 使用配对t检验（因为是被试内设计）

**解读:**
- **p < 0.05 且 exag > non_exag**: 夸张加剧了讽刺处理困难
- **p < 0.05 且 exag < non_exag**: 夸张促进了讽刺处理（更容易识别）
- **p > 0.05**: 夸张对讽刺处理无显著影响

---

## 🎯 核心研究问题

### 问题1：夸张的讽刺句和不夸张的讽刺句处理模式是否不同？
**查看:** `model_exaggeration` 中的 `type:exaggeration` 交互项

**理论预测:**
- **促进假说:** 夸张使讽刺更明显 → 负向交互（讽刺效应变小或消失）
- **干扰假说:** 夸张增加处理负担 → 正向交互（讽刺效应增大）

---

### 问题2：讽刺效应在哪种条件下更强？
**查看:** 描述性统计表和t检验结果

**可能结果:**
| 情况 | 意义 |
|------|------|
| 有夸张 > 无夸张 | 夸张让讽刺更难处理（需要解析双重意义） |
| 有夸张 < 无夸张 | 夸张提供了线索，帮助识别讽刺（促进效应） |
| 有夸张 ≈ 无夸张 | 夸张不影响讽刺处理 |

---

### 问题3：夸张与陈述方式（直接/间接）是否有交互？
**查看:** `model_exaggeration` 中的 `format:type:exaggeration` 三阶交互

**例如:**
- 直接引语中，夸张可能更突出
- 间接引语中，夸张效应可能被削弱

---

## 📊 如何解读结果

### 混合效应模型输出

```
Fixed effects:
                           Estimate Std.Error t-value
(Intercept)                  450.23    15.42   29.19
format1                       12.45     8.31    1.50
type1                         28.67     9.15    3.13 **
exaggeration1                 -5.32     7.89   -0.67
format1:type1                  8.21     6.45    1.27
format1:exaggeration1          2.10     5.78    0.36
type1:exaggeration1          -15.43     6.92   -2.23 *  ← 关键！
format1:type1:exaggeration1    3.45     5.21    0.66
```

**重点关注:**
- **`type1:exaggeration1`** = 夸张 × 讽刺交互
  - 显著负值：夸张减弱了讽刺效应（促进识别）
  - 显著正值：夸张增强了讽刺效应（增加困难）

---

### 描述性统计示例解读

```
  format    type       exaggeration     mean_RT
  direct    literal    non-exaggerated  430
  direct    literal    exaggerated      445
  direct    sarcastic  non-exaggerated  455
  direct    sarcastic  exaggerated      448
```

**计算讽刺效应:**
- 无夸张：455 - 430 = 25 ms（讽刺比字面慢）
- 有夸张：448 - 445 = 3 ms（讽刺与字面几乎一样快）

**结论:** 夸张减少了讽刺处理困难（促进效应）

---

## 🔧 如何运行分析

### 前提条件
1. R 和 RStudio 已安装
2. 安装必要的包：
```r
install.packages("tidyverse")
install.packages("buildmer")
install.packages("tinytable")
```

### 运行步骤

#### 方法1：在RStudio中运行
1. 打开 `2025.5.6_analysis_with_exaggeration.qmd`
2. 点击 "Render" 按钮
3. 等待生成HTML报告

#### 方法2：命令行运行
```bash
quarto render 2025.5.6_analysis_with_exaggeration.qmd
```

#### 方法3：逐块运行（推荐，用于调试）
1. 在RStudio中打开.qmd文件
2. 使用 Ctrl+Shift+Enter 逐个运行代码块
3. 查看每一步的输出

---

## 📈 预期输出

运行成功后，你将得到：

### 1. 描述性统计表
- 各条件下的平均反应时间
- 讽刺效应大小（按夸张分组）

### 2. 可视化图表
- 交互图：显示夸张如何调节讽刺处理
- 柱状图：直接对比讽刺效应大小

### 3. 统计模型结果
- 原始模型（format × type）
- 扩展模型（format × type × exaggeration）
- 聚焦模型（type × exaggeration）
- 过滤后模型（仅"好"项目）

### 4. t检验结果
- 配对样本比较：有夸张 vs 无夸张

### 5. 项目分析
- 每个项目的讽刺效应
- 按夸张分组的项目质量评估

---

## ⚠️ 注意事项

### 1. 文件名问题
原代码中有：
```r
stimuli <- read_csv('stimuli_corrected.csv')
```
但实际文件名是 `stimuli.csv`。新代码已修正。

### 2. 对比编码 (Contrast Coding)
```r
contrasts(edata$exaggeration) = contr.sum(2)
```
- 使用**sum contrasts** (-0.5, +0.5)
- 使主效应在有交互时也可解释
- 不要改成 `contr.treatment` 除非你清楚影响

### 3. 模型收敛问题
如果模型不收敛，尝试：
```r
# 简化随机效应
model <- buildmer(
  RT ~ format * type * exaggeration +
    (1 | id) +  # 只有随机截距
    (1 | item_name),
  data = edata
)
```

### 4. 样本量问题
- 确保每个条件至少有 20+ 观测值
- 如果某些参与者数据缺失过多，考虑排除

---

## 🎓 理论背景

### 为什么研究夸张在讽刺中的作用？

1. **夸张是讽刺的常见特征**
   - "那真是*太棒了*！"（对糟糕表现的评价）
   - 强度词（terrific, awful, amazing）增强讽刺语气

2. **两种理论预测**
   - **标记假说 (Marking hypothesis):** 夸张是讽刺的显性标记，帮助听者识别非字面意义
   - **负担假说 (Burden hypothesis):** 夸张增加语义处理复杂度，延长反应时间

3. **实证证据混合**
   - 一些研究发现夸张促进讽刺理解
   - 另一些研究发现夸张无影响甚至增加困难

### 本研究的贡献
- 在**自然阅读**（自定步速）范式下测试夸张效应
- 控制**陈述方式**（直接引语 vs 间接引语）
- 提供**项目层面分析**（识别哪些材料有效）

---

## 📚 延伸分析建议

如果结果显著，可以进一步分析：

### 1. 按年龄分组
```r
edata <- edata |> mutate(age_group = ifelse(age < 25, "young", "old"))
model_age <- buildmer(RT ~ type * exaggeration * age_group + ...)
```

### 2. 按准确率分组（高/低理解者）
```r
edata <- edata |> mutate(accuracy_group = ifelse(accuracy > 0.85, "high", "low"))
```

### 3. 溢出效应 (Spillover effects)
```r
# 分析critical region之后的区域
edata_spillover <- edata |> filter(region == critical_region + 1)
```

### 4. 残差阅读时间 (RRT) 分析
```r
# 用RRT代替RT重新运行所有模型
model_RRT <- buildmer(RRT ~ format * type * exaggeration + ...)
```

---

## 🆘 常见问题

### Q1: 模型报错 "boundary (singular) fit"
**A:** 随机效应结构过于复杂，简化为：
```r
(1|id) + (1|item_name)  # 只有截距
```

### Q2: 某些条件的n很小
**A:** 检查数据：
```r
edata |> count(format, type, exaggeration)
```
考虑排除n < 10的条件。

### Q3: 如何报告结果？
**A:** 标准格式：
> "We found a significant type × exaggeration interaction (β = -15.43, SE = 6.92, t = -2.23, p = .028), indicating that the sarcasm effect was reduced in exaggerated items (3 ms) compared to non-exaggerated items (25 ms)."

### Q4: 如何可视化三阶交互？
**A:** 创建分面图：
```r
ggplot(desc_stats, aes(x=type, y=mean_RT, color=exaggeration)) +
  geom_line(aes(group=exaggeration)) +
  facet_grid(format ~ .)
```

---

## 📧 总结

这个修改后的分析可以回答：

✅ **夸张是否影响讽刺处理速度**
✅ **这种影响是促进还是阻碍**
✅ **夸张效应是否取决于陈述方式（直接/间接）**
✅ **哪些实验材料最能展示夸张效应**

通过结合描述性统计、可视化和混合效应模型，你可以全面理解夸张在讽刺理解中的作用。

---

**创建日期:** 2025-11-23
**作者:** Claude
**版本:** 1.0
